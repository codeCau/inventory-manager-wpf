<ui:FluentWindow x:Class="ControleEstoqueWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:converters="clr-namespace:ControleEstoqueWPF.Converters"
        mc:Ignorable="d"
        Title="Controle de Estoque &amp; Produtos" 
        Height="890" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:EstoqueBadgeConverter x:Key="EstoqueBadgeConverter"/>
        <converters:EstoqueColorConverter x:Key="EstoqueColorConverter"/>
    </Window.Resources>

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ui:InfoBar Grid.Row="0" 
                        IsOpen="{Binding IsInfoBarOpen, Mode=TwoWay}" 
                        Title="{Binding InfoBarTitle}" 
                        Message="{Binding InfoBarMessage}" 
                        Severity="{Binding InfoBarSeverity}" 
                        Margin="0,0,0,12" />

            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <ui:TextBlock Text="📦 Gestão Integrada de Estoque &amp; Inventário" FontTypography="Title" />
                <ui:TextBlock Text="Painel em tempo real com controle de rupturas, métricas financeiras e fluxo operacional." FontTypography="Body" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ui:Card Grid.Column="0" Margin="0,0,8,0" Padding="12">
                    <StackPanel>
                        <ui:TextBlock Text="Patrimônio em Estoque" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBlock Text="{Binding ValorTotalEstoque, StringFormat=C2}" FontTypography="Subtitle" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="1" Margin="0,0,8,0" Padding="12">
                    <StackPanel>
                        <ui:TextBlock Text="Total de Unidades Físicas" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBlock Text="{Binding QuantidadeTotalItens, StringFormat='{}{0} un.'}" FontTypography="Subtitle" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2" Margin="0,0,8,0" Padding="12">
                    <StackPanel>
                        <ui:TextBlock Text="Preço Médio do Catálogo" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBlock Text="{Binding PrecoMedioUnitario, StringFormat=C2}" FontTypography="Subtitle" FontWeight="Bold" Margin="0,4,0,0"/>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="3" Padding="12">
                    <StackPanel>
                        <ui:TextBlock Text="Alertas de Reposição" FontTypography="Caption" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        <ui:TextBlock Text="{Binding TotalCriticos, StringFormat='{}{0} itens críticos'}" FontTypography="Subtitle" FontWeight="Bold" Foreground="#DC3545" Margin="0,4,0,0"/>
                    </StackPanel>
                </ui:Card>
            </Grid>

            <ui:Card Grid.Row="3" Margin="0,0,0,16" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                        <ui:TextBlock Text="Nome do Produto *" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <ui:TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Ex: Memória RAM DDR4 8GB" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                        <ui:TextBlock Text="Preço Unitário (R$) *" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <ui:TextBox Text="{Binding Preco, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="0.00" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                        <ui:TextBlock Text="Quantidade em Estoque *" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <ui:TextBox Text="{Binding QuantidadeEstoque, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="0" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Margin="0,0,0,12">
                        <ui:TextBlock Text="Descrição Detalhada" FontTypography="BodyStrong" Margin="0,0,0,4"/>
                        <ui:TextBox Text="{Binding Descricao, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Especificações adicionais do produto..." />
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Content="Limpar Formulário" Command="{Binding LimparFormularioCommand}" Appearance="Secondary" Margin="0,0,8,0"/>
                        <ui:Button Content="Salvar Registro" Command="{Binding SalvarProdutoCommand}" Appearance="Primary" Icon="{ui:SymbolIcon Symbol=Save24}"/>
                    </StackPanel>
                </Grid>
            </ui:Card>

            <Grid Grid.Row="4" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ui:TextBox Grid.Column="0" Text="{Binding TermoPesquisa, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Pesquisar por nome ou descrição..." Icon="{ui:SymbolIcon Symbol=Search24}"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="{Binding TotalGeral, StringFormat='Todos ({0})'}" Command="{Binding FiltrarPorStatusCommand}" CommandParameter="Todos" Appearance="Secondary" Margin="0,0,6,0"/>
                    <ui:Button Content="{Binding TotalCriticos, StringFormat='Críticos ({0})'}" Command="{Binding FiltrarPorStatusCommand}" CommandParameter="Critico" Appearance="Danger" Margin="0,0,6,0"/>
                    <ui:Button Content="{Binding TotalNormais, StringFormat='Normais ({0})'}" Command="{Binding FiltrarPorStatusCommand}" CommandParameter="Normal" Appearance="Success" Margin="0,0,6,0"/>
                    <ui:Button Content="{Binding TotalExcedentes, StringFormat='Excedentes ({0})'}" Command="{Binding FiltrarPorStatusCommand}" CommandParameter="Excedente" Appearance="Caution" Margin="0,0,12,0"/>
                    <ui:Button Content="Exportar CSV" Command="{Binding ExportarCsvCommand}" Appearance="Secondary" Icon="{ui:SymbolIcon Symbol=DocumentTableSearch24}"/>
                </StackPanel>
            </Grid>

            <ui:Card Grid.Row="5" Padding="0">
                <DataGrid ItemsSource="{Binding Produtos}" SelectedItem="{Binding ProdutoSelecionado}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Produto" Binding="{Binding Nome}" Width="2*"/>
                        <DataGridTextColumn Header="Preço" Binding="{Binding Preco, StringFormat=C2}" Width="110"/>
                        
                        <DataGridTemplateColumn Header="Estoque" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <ui:Button Content="-" Width="24" Height="24" Padding="0" Appearance="Secondary"
                                                   Command="{Binding DataContext.DecrementarEstoqueCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                   CommandParameter="{Binding}" Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding QuantidadeEstoque}" Width="30" TextAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                        <ui:Button Content="+" Width="24" Height="24" Padding="0" Appearance="Secondary"
                                                   Command="{Binding DataContext.IncrementarEstoqueCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                   CommandParameter="{Binding}" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Status do Estoque" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding QuantidadeEstoque, Converter={StaticResource EstoqueColorConverter}}" CornerRadius="4" Padding="8,3" HorizontalAlignment="Left" Margin="4,2">
                                        <TextBlock Text="{Binding QuantidadeEstoque, Converter={StaticResource EstoqueBadgeConverter}}" Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Ações" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <ui:Button Icon="{ui:SymbolIcon Symbol=Edit24}" Appearance="Transparent" ToolTip="Editar Registro" Command="{Binding DataContext.PrepararEdicaoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="0,0,4,0"/>
                                        <ui:Button Icon="{ui:SymbolIcon Symbol=Delete24}" Appearance="Transparent" ToolTip="Excluir Produto" Command="{Binding DataContext.ExcluirProdutoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </ui:Card>
        </Grid>

        <ContentPresenter x:Name="RootContentDialogPresenter" />
    </Grid>
</ui:FluentWindow>
